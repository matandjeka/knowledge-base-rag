RAG_API_URL=http://127.0.0.1:8000
FRONTEND_ORIGIN=http://localhost:3000
# Same strong random value as the FastAPI setting; server-side only.
WORKFLOW_SECRET=
BLOB_READ_WRITE_TOKEN=
# Optional deployment protection bypass for calls between preview projects.
VERCEL_AUTOMATION_BYPASS_SECRET=
